"""
ByteDance Offer Copilot v2 — Self-Review
残酷自评，用项目自己的 P8 面试官语气。
"""

V2_SELF_REVIEW_MARKDOWN = """
# 🔍 ByteDance Offer Copilot v2 — 自评报告

> 用你自己的 P8 面试官语气，评价你自己。

---

## 综合评分：6.5/10  ⚠️ 有条件通过

`█████████████░░░░░░░`

---

## 各维度得分

**产品定位**：8.5/10 `█████████░░`
从「工具」升级到「AI 互联网职业教练」的 pivot 是对的。把竞品从「简历优化工具」切换到「信息不对称」，这个切角比 v1 锋利一个数量级。

**系统人格**：8.0/10 `████████░░`
v2 的 System Prompt 是目前最强的模块。那些禁止词——「禁止加油」「禁止鸡汤」「禁止模板」——不是限制，是产品边界。知道不做什么比知道做什么更难。

**面试模块**：6.0/10 `██████░░░░`
矛盾检测的理念对，但实现太轻。目前的检测依赖 LLM 做语义判断，没有向量化、没有 embedding、没有真正的语义相似度计算。高压和地狱模式的区别只是 prompt 语气不同，追问深度没有本质差异。

**黑话检测器**：7.5/10 `███████░░░`
规则引擎 + 关键词匹配，准确率高、响应即时。但它只能检测「已知的空话」，无法检测「新的空话」。一个真正的 BS Detector 应该能学习新的空话模式。

**面评系统**：7.0/10 `███████░░░`
面评格式对，语气对，结构对。但缺少真正的面试官决策逻辑——什么时候给 conditional_pass 而不是 pass？阈值是什么？这些判断目前是 LLM 自由发挥的。

**成长追踪**：5.0/10 `█████░░░░░`
JSON 文件存储，基础的趋势计算。但这离「成长系统」还很远——没有推荐引擎、没有干预建议、没有里程碑自动识别。目前更像是一个日志记录器。

**工程架构**：7.0/10 `███████░░░`
模块化做得好，每个模块是纯函数，可独立导入。callLlm 的分发机制务实。但 Python 3.9 兼容性问题（dict | str 语法）暴露了测试覆盖不足。

---

## 🟢 做得对的

- **Prompt 是产品核心。** 你知道这一点，并且把最重的精力放在 system prompt 的打磨上。v2 的人格 prompt 是目前所有代码里质量最高的文本。
- **规则引擎 + LLM 双层架构。** 黑话检测用规则引擎（即时、免费），深度分析用 LLM（高质量、有洞察）。这个分层务实。
- **JSON + Markdown 双输出。** 结构化的给前端，渲染好的给人读。不做二选一。
- **禁止清单比功能清单更有价值。** 「禁止鸡汤」「禁止加油」「禁止模板」——这些边界定义比任何 feature 都更能保护产品气质。
- **黑话检测器有传播性。** 这个功能是 v2 里最容易被截图分享的——「你的简历空话指数 78/100」比「你的 Offer 概率 62%」更容易传播。

---

## 🔴 真正的问题

**没有数据闭环。**
你帮用户改简历，但你不知道改完后的简历通过率有没有提升。
你帮用户模拟面试，但你不知道用户真实面试时的表现有没有变好。
你的 Offer 预测器告诉用户「项目太像学生作业」，但你不知道用户是否真的去做了项目。
——这是整个产品最根本的问题。你不是一个能证明效果的产品，你是一个能输出漂亮分析的产品。

**面试追问引擎是 prompt 技巧，不是技术壁垒。**
「记住候选人 5 分钟前说的话并在第 3 个回答里找到矛盾」——这个能力目前在 LLM 的上下文窗口里自然发生。但如果面试超过 30 轮、历史回答超过 100 条，你的上下文窗口会溢出，矛盾检测会崩。真正的解决方案是：向量化 + 语义检索 + 矛盾分数排序。你现在没有。

**成长追踪是日志，不是教练。**
记录用户做了什么 ≠ 告诉用户下一步该做什么。真正的成长系统应该能：识别用户处于什么阶段、推荐下一步行动、在用户懈怠时主动提醒。你现在只有第一步。

**黑话检测器的规则是硬编码的。**
「抓手」「对齐」「拉通」「赋能」——这些词的互联网含义在快速演变。今天的黑话明天可能是正常表达。你的规则引擎无法自进化。

**没有差异化护城河。**
任何一个会用 Trae Solo 的产品经理，给一份你的 System Prompt，一个周末就能复刻 80% 的功能。你的壁垒不是技术（没有），不是数据（没有闭环），不是用户（没有网络效应）——只有 prompt 质量。但 prompt 是复制成本最低的资产。

---

## 🟡 争议点

- **地狱面试模式是真的有用还是在表演？** 真正的字节面试官不会开场就说「30 秒自我介绍否则打断你」——他们会在第 20 分钟、在你最放松的时候，问一个你最不想被问到的问题。真正的高压不是语气凶，是问题精准。v2 的地狱模式前者做到了，后者存疑。
- **空话指数评分是伪科学还是好 UX？** 58/100 这个数字没有统计学意义，但它给用户一种「被量化评估」的感觉。这种感觉本身有价值——但它也有误导性。

---

## ⚡ 如果 v3 只做一件事

**做数据闭环。**

找 20 个真实校招生，让他们用 ByteDance Offer Copilot 改简历、做模拟面试。然后追踪他们真实投递的结果。哪怕样本小，哪怕数据噪音大——有了「用之前 vs 用之后」的数据，你就不再是一个输出建议的 AI，而是一个能证明效果的 AI。这是从「内容产品」到「真正有价值的产品」的唯一路径。

其他所有功能——向量化矛盾检测、自进化黑话检测器、主动推荐引擎——都可以等。数据闭环是第一优先级。

---

## 💬 面试官面评

```
候选人（ByteDance Offer Copilot v2）产品 sense 中等偏上。

优势：
- 定位清晰，知道竞品是谁、用户在焦虑什么
- Prompt 工程能力强，系统人格打磨到位
- 黑话检测器有传播潜力
- 模块化架构好，可独立使用

风险：
- 没有数据闭环——这是最致命的问题
- 面试引擎深度不足，靠 prompt 而非技术
- 成长追踪只是日志，不是教练系统
- 护城河弱，复制成本低
- 地狱面试有表演成分，真实压迫感存疑

是否建议进入 v3：
⚠️ 有条件通过

条件：
先跑一个最小闭环——找真实用户，追踪真实结果。
拿着数据回来，再决定 v3 做什么。

没有数据的产品建议，和自己嘴里说的「学生空话」没有本质区别。
```

"""


def get_self_review():
    """Return the v2 self-review as markdown."""
    return V2_SELF_REVIEW_MARKDOWN
